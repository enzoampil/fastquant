matrix:
  include:
    - language: python
      before_script:
        - cd python
      script:
        - pytest tests
    - language: r
      before_script:
        - cd R
      sudo: false
      cache: packages
